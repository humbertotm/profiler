(ns screener.models)

(def records-map {:sub "Sub"
                  :tag "Tag"
                  :num "Num"
                  :pre "Pre"})

(def tables {:sub :submissions
             :tag :tags
             :num :numbers
             :pre :presentations})

